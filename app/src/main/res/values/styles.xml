<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="TimePickerTheme" parent="ThemeOverlay.MaterialComponents.TimePicker">
        <item name="android:timePickerMode">spinner</item>

        // 버튼 색상
        <item name="colorAccent">@color/main_darker</item>
        <item name="android:textColorPrimary">@color/main_darker</item>

        // 배경 색상
        <item name="android:background">@color/white_background</item>

        // 디바이더 색상
        <item name="colorControlNormal">@color/main_darker</item>

        <item name="android:textStyle">bold</item>
        <item name="android:padding">4dp</item>
        <item name="android:textSize">18sp</item>

    </style>

</resources>